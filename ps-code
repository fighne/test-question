#pseudo code
# haven't used c#

#function steps

1. test for string => f("str") => t | f => ternary test message on fail "try again, but a string this time like 'test'"
# looking on W3 schools c-sharp can use a variable type to check for string, if fail catch exception

2. reverse the input f("nepo") => "open"
# c-sharp has built in str to array function - use that
# c-sharp has built in reverse array - use that
# c-sharp has built in concat of array to return a str - use that


3. find the 'earliest' letter in the string f(reversed_str) => run a quick sort based on aA = 1 ...... zZ = 26 return the first position in the returned array
# c-sharp has built in 'tolower' function - use that and reduces having to figure out aA 
# c-sharp use str to array
# c-sharp use array.sort
# c-sharp use array[0] to return the first value

4. take reverse string and convert to an array f("str") => ["s", "t", "r"], interact over the array check each point for comparision aA,eE,iI,oO,uU if true add one to a count
check for odd/even (modulo of 2) => t|f => rent|open
# c-sharp use 'tolower'
# c-sharp use str to array
# c-sharp use array foreach loop
# c-sharp feed output of foreach in conditional of equals 'a|e|i|o|u' add 1 to count
# c-sharp int odd even check using % =>  int % 2
# c-sharp has a ToBoolean - use that the convert int to true|false
# test and return open|rent

5. output_str = point 2 + point 3 + point 4
# c-sharp use concat to add the strings to gether for output

#nb.
# can see a number of optimisations already but will leave it here  
